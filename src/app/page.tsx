import { TimeCalculator } from "@/app/Components";

const HomePage = () => {
  return (
    <section className="grid place-items-center min-h-dvh w-full">
      
      <TimeCalculator />
    </section>
  );
};

export default HomePage;
