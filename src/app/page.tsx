import { TimeCalculator } from "@/app/Components";
export const metadata = {
  title: "Time Entry Calculator",
  description: "Calculate the total sum of multiple time durations.",
};

const HomePage = () => {
  return <TimeCalculator />;
};

export default HomePage;
